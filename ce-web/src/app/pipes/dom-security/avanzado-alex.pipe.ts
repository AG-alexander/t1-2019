import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: 'avanzadoAlex'
})
export class AvanzadoAlexPipe implements PipeTransform {

  transform(value: any, args?: any): any {
    return null;
  }

}
