import { AvanzadoAlexPipe } from './avanzado-alex.pipe';

describe('AvanzadoAlexPipe', () => {
  it('create an instance', () => {
    const pipe = new AvanzadoAlexPipe();
    expect(pipe).toBeTruthy();
  });
});
